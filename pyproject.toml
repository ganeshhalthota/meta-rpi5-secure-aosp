[project]
name = "meta-rpi5-secure-aosp"
version = "0.1.0"
description = ""
authors = [
    {name = "Ganesh Halthota",email = "cs23m510@smail.iitm.ac.in"}
]
readme = "README.md"
requires-python = ">=3.10,<3.15"
dependencies = [
    "rich (>=14.2.0,<15.0.0)",
    "pyyaml (>=6.0.3,<7.0.0)",
    "click (>=8.3.1,<9.0.0)",
    "tqdm (>=4.67.1,<5.0.0)",
    "psutil (>=7.1.3,<8.0.0)"
]

# [tool.poetry]
# packages = [{include = "meta_rpi5_secure_aosp", from = "src"}]

[tool.poetry.scripts]
rpi5-build = "meta_rpi5_secure_aosp.main:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pyinstaller (>=6.17.0,<7.0.0)"
]
